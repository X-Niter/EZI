package com.knoxhack.ezi;

public final class OverlayManager {
    /*
    public static void init() {
        // Listen on the NeoForge EVENT_BUS for InputEvent.Key
        NeoForge.EVENT_BUS.addListener(
                EventPriority.NORMAL,
                false,
                InputEvent.Key.class,
                OverlayManager::onKey
        );
    }

    private static void onKey(InputEvent.Key evt) {
        // When our keybinding fires, open our EZI screen
        if (Keys.OPEN_GUI.consumeClick()) {
            Minecraft.getInstance().setScreen(new ItemListScreen());
        }
    }

     */
}